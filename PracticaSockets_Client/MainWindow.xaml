<Window x:Class="PracticaSockets_Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:PracticaSockets_Client.ViewModels"
        xmlns:views="clr-namespace:PracticaSockets_Client.Views.Tabs"
        mc:Ignorable="d"
        Title="Cliente - Práctica Sockets" Height="600" Width="900"
        Background="#181825" WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <!-- DataTemplates para conectar ViewModels con Views automáticamente -->
        <DataTemplate DataType="{x:Type viewmodels:Tab1ViewModel}">
            <views:Tab1_Configuracion />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:Tab2ViewModel}">
            <views:Tab2_NoBloqueante />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:Tab3ViewModel}">
            <views:Tab3_MultiHilos />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:Tab4ViewModel}">
            <views:Tab4_Archivos />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:Tab5ViewModel}">
            <views:Tab5_Ssl />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:Tab6ViewModel}">
            <views:Tab6_Chat />
        </DataTemplate>

        <!-- Estilo moderno para el TabControl -->
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="#1E1E2E"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="10"/>
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#89B4FA"/>
                    <Setter Property="Foreground" Value="#11111B"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Cursor" Value="Hand"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <TabControl x:Name="MainTabControl">
            <TabItem Header="1. Config">
                <ContentPresenter Content="{Binding Tab1}" />
            </TabItem>
            <TabItem Header="2. No Bloqueante">
                <ContentPresenter Content="{Binding Tab2}" />
            </TabItem>
            <TabItem Header="3. Multi-Hilos">
                <ContentPresenter Content="{Binding Tab3}" />
            </TabItem>
            <TabItem Header="4. Archivos">
                <ContentPresenter Content="{Binding Tab4}" />
            </TabItem>
            <TabItem Header="5. SSL/TLS">
                <ContentPresenter Content="{Binding Tab5}" />
            </TabItem>
            <TabItem Header="6. Chat">
                <ContentPresenter Content="{Binding Tab6}" />
            </TabItem>
        </TabControl>
    </Grid>
</Window>
