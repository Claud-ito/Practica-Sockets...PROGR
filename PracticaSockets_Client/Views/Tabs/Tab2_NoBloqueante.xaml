<UserControl x:Class="PracticaSockets_Client.Views.Tabs.Tab2_NoBloqueante"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#1E1E2E" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Sockets No Bloqueantes (Async/Await)" Foreground="#CDD6F4" FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="Probar Conexión Asíncrona" Command="{Binding TestAsyncCommand}" IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}, FallbackValue=True}" Background="#A6E3A1" Foreground="#11111B" FontWeight="Bold" BorderThickness="0" Padding="15,8" Cursor="Hand" Margin="0,0,15,0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>
            <ProgressBar IsIndeterminate="{Binding IsBusy}" Width="150" Height="10" Foreground="#89B4FA" Background="#313244" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Visible}"/>
            <TextBlock Text="¡La UI no se congela mientras espera!" Foreground="#F9E2AF" VerticalAlignment="Center" Margin="15,0,0,0" FontStyle="Italic"/>
        </StackPanel>

        <Border Grid.Row="2" Background="#11111B" CornerRadius="8" Padding="15">
            <TextBlock Text="{Binding Log}" Foreground="#89DCEB" TextWrapping="Wrap" FontFamily="Consolas"/>
        </Border>
    </Grid>
</UserControl>
