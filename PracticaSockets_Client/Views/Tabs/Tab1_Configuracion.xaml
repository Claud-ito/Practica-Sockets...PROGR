<UserControl x:Class="PracticaSockets_Client.Views.Tabs.Tab1_Configuracion"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#1E1E2E" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Configuración Básica de Sockets TCP" Foreground="#CDD6F4" FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Text="Dirección IP:" Foreground="#A6ADC8" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Grid.Column="1" Text="{Binding Ip, UpdateSourceTrigger=PropertyChanged}" Background="#313244" Foreground="White" BorderThickness="0" Padding="8" Margin="0,0,20,0"/>

            <TextBlock Grid.Column="2" Text="Puerto:" Foreground="#A6ADC8" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Grid.Column="3" Text="{Binding Puerto, UpdateSourceTrigger=PropertyChanged}" Background="#313244" Foreground="White" BorderThickness="0" Padding="8"/>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBox Text="{Binding Mensaje, UpdateSourceTrigger=PropertyChanged}" Width="300" Background="#313244" Foreground="White" BorderThickness="0" Padding="8" Margin="0,0,10,0"/>
            <Button Content="Conectar y Enviar" Command="{Binding ConectarCommand}" IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}, FallbackValue=True}" Background="#89B4FA" Foreground="#11111B" FontWeight="Bold" BorderThickness="0" Padding="15,8" Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>

        <Border Grid.Row="3" Background="#11111B" CornerRadius="8" Padding="15">
            <TextBlock Text="{Binding Log}" Foreground="#A6E3A1" TextWrapping="Wrap" FontFamily="Consolas"/>
        </Border>
    </Grid>
</UserControl>
